name: Build and Deploy

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          elif [ -f yarn.lock ]; then
            yarn install --frozen-lockfile
          elif [ -f package.json ]; then
            npm install
          fi
          
      - name: Build
        run: |
          if npm run build --if-present; then
            echo "Build successful or no build script found."
          else
            echo "Build failed."
            exit 1
          fi

  deploy-preview:
    if: github.event_name == 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Preview Deployment Placeholder
        run: echo "Preview deployment logic goes here (e.g., Vercel Preview, Surge, etc.)"

  deploy-prod:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Production Deployment Placeholder
        run: echo "Production deployment logic goes here (e.g., Vercel, GitHub Pages, etc.)"
